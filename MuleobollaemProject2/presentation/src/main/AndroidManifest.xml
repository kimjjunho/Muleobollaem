<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muleobollaemproject2">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name=".di.Application"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuleobollaemProject2">
        <activity
            android:name=".feature.see.ui.SeeActivity"
            android:exported="false" />
        <activity
            android:name=".feature.write.WriteActivity"
            android:exported="false" />
        <activity
            android:name=".feature.main.ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".feature.login.LoginBaseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>